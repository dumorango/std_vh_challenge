import React from 'react';
import {LoginContainer} from "../";

export const LoginPage = () => (
    <div style={{
        display: 'grid',
        justifyContent: 'center',
        alignContent: 'center'
    }} >
        <LoginContainer />
    </ div>
);